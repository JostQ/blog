<div class="row justify-content-center">
    <div class="col-7">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-dark">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
</div>
